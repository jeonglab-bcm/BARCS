% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{barcs_cpm}
\alias{barcs_cpm}
\title{Counts per million}
\usage{
barcs_cpm(counts, totals = NULL)
}
\arguments{
\item{counts}{Guide-by-sample numeric matrix or data frame of raw counts.}

\item{totals}{Optional per-sample library totals. Defaults to the column
sums of \code{counts}, which is only correct when \code{counts} is unfiltered.}
}
\value{
A numeric matrix of the same shape as \code{counts}, in counts per
million.
}
\description{
Scales each column of a count matrix by its own column sum. This is a
presentation and diagnostic scale only. Never pass CPM values to
\code{\link[=bb_screen]{bb_screen()}}: the beta-binomial denominator has to be an integer count of
sequenced reads.
}
\examples{
counts <- cbind(a = c(10, 30, 60), b = c(20, 20, 60))
barcs_cpm(counts)
}
