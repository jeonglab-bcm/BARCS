% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{barcs_quant_cpp}
\alias{barcs_quant_cpp}
\title{Count guide barcodes in FASTQ files}
\usage{
barcs_quant_cpp(library_path, fastq_path, verbose = FALSE)
}
\arguments{
\item{library_path}{Path to the guide library FASTA file.}

\item{fastq_path}{Character vector of plain-text FASTQ paths. Compressed
input must be expanded by the caller first.}

\item{verbose}{Write progress to the standard error stream.}
}
\value{
A list with the guide names and sequences, the guide-by-sample
count matrix, per-sample read and mapped-read totals, a per-sample
reverse-complement flag, the detected guide length, and the number of
library entries dropped as repeated, off-length, or non-ACGT.
}
\description{
Compiled entry point behind \code{\link[=barcs_quant]{barcs_quant()}}. It builds the guide index
once and scans each FASTQ file in turn, so the library FASTA is parsed a
single time no matter how many samples are supplied.
}
\keyword{internal}
