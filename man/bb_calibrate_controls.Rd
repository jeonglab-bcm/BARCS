% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{bb_calibrate_controls}
\alias{bb_calibrate_controls}
\title{Calibrate guide-level t tests with negative-control guides}
\usage{
bb_calibrate_controls(
  result,
  control,
  alpha = 0.05,
  min_controls = 20L,
  min_scale = 1,
  method = c("tail_quantile", "qq_slope")
)
}
\arguments{
\item{result}{A guide-level data frame returned by \code{\link[=bb_screen]{bb_screen()}}.}

\item{control}{Logical vector identifying negative-control guides, one
element per row of \code{result}.}

\item{alpha}{Tail probability at which to estimate the null scale.}

\item{min_controls}{Minimum number of finite negative controls required.}

\item{min_scale}{Lower bound for the estimated scale.}

\item{method}{Null-scale estimator. \code{"tail_quantile"} (default) matches the
empirical \code{(1 - alpha)} quantile of the absolute control statistics to the
corresponding two-sided Student t cutoff. \code{"qq_slope"} instead fits the
slope of the control quantile-quantile plot against the t reference over
the 0.50 to 0.95 probability band, which uses many order statistics rather
than one and is markedly less variable.}
}
\value{
\code{result} with recalibrated standard errors, t statistics, p-values,
and FDR. Original inferential columns are retained with a \code{raw_} prefix;
the estimated scale and \code{alpha} are stored as the attributes
\code{"control_scale"} and \code{"control_alpha"}.
}
\description{
Estimates a one-parameter empirical-null scale from the absolute
negative-control t statistics. The scale matches their empirical
\code{(1 - alpha)} quantile to the corresponding two-sided Student t cutoff.
Scales below \code{min_scale} are truncated, so calibration need not make an
already conservative analysis more liberal.
}
\details{
All finite control guides must share one residual degrees-of-freedom value,
which holds when every guide is fitted with the same complete design.
Heterogeneous guide designs need separate calibration strata.
}
\examples{
# A control set whose null t statistics are inflated by a factor of 1.5.
set.seed(5)
n <- 400
result <- data.frame(
  estimate = rnorm(n),
  std_error = 1,
  t_value = 1.5 * rt(n, df = 10),
  df = 10
)
result$p_value <- 2 * pt(-abs(result$t_value), df = 10)
result$fdr <- p.adjust(result$p_value, method = "BH")

calibrated <- bb_calibrate_controls(result, control = rep(TRUE, n))
attr(calibrated, "control_scale")
}
\seealso{
Other recalibration: 
\code{\link{bb_moderate_dispersion}()}
}
\concept{recalibration}
