% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bbreg.R
\name{bb_contrast}
\alias{bb_contrast}
\title{Test a linear contrast of beta-binomial regression coefficients}
\usage{
bb_contrast(object, contrast, null = 0)
}
\arguments{
\item{object}{A fitted \code{bbreg} object.}

\item{contrast}{Numeric contrast vector in coefficient order, or a named
numeric vector whose names identify coefficients. Unnamed coefficients in
a named contrast are given weight zero.}

\item{null}{Null value for the contrast.}
}
\value{
A one-row data frame with columns \code{estimate}, \code{std_error},
\code{t_value}, \code{df}, and \code{p_value}.
}
\description{
Forms \verb{c'beta} for a user-supplied contrast vector, takes its standard
error from the fitted coefficient covariance, and tests it against the same
Student t reference \code{\link[=bbreg]{bbreg()}} uses for individual coefficients.
}
\examples{
set.seed(3)
design <- data.frame(arm = factor(rep(c("ctrl", "low", "high"), each = 4)))
total <- rep(30000L, 12)
count <- rbinom(12, total, plogis(-6.5 + c(0, 0.4, 0.9)[design$arm]))
fit <- bbreg(count, total, ~ arm, design)

# High versus low, given the model's treatment contrasts.
bb_contrast(fit, c(armlow = -1, armhigh = 1))
}
\seealso{
Other guide-level modelling: 
\code{\link{bb_screen}()},
\code{\link{bbreg}()},
\code{\link{bbreg-methods}}
}
\concept{guide-level modelling}
