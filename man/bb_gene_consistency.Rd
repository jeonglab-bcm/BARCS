% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene.R
\name{bb_gene_consistency}
\alias{bb_gene_consistency}
\title{Test a shared guide effect against an empirical gene-level null}
\usage{
bb_gene_consistency(
  result,
  control = NULL,
  min_guides = 3L,
  alpha = 0.05,
  min_control_genes = 10L,
  min_scale = 1
)
}
\arguments{
\item{result}{A guide-level result from \code{\link[=bb_screen]{bb_screen()}}. If control
calibration has already been applied, the retained \code{raw_std_error}
column is used automatically.}

\item{control}{Optional non-missing logical vector identifying control
guides. A control gene must contain only control guides.}

\item{min_guides}{Minimum number of finite guide scores required per gene.}

\item{alpha}{Tail probability used to estimate the control-gene scale.}

\item{min_control_genes}{Minimum number of valid control genes needed to
use their median and tail scale.}

\item{min_scale}{Lower bound for the empirical-null scale.}
}
\value{
A data frame with one row per testable gene. \code{statistic} is the
empirical-null standardized gene score and \code{p_value} uses a standard
normal reference. The null parameters are also stored as the attributes
\code{"null_center"}, \code{"null_scale"}, \code{"global_scale"}, \code{"control_scale"}, and
\code{"control_genes"}.
}
\description{
This function is intended for exploratory screens in which several
independently designed guides target each gene but biological replication
is too limited for reliable guide-level reference distributions. It does
not treat guides as biological replicates. Instead, it estimates one shared
gene effect by inverse-variance weighting of guide coefficients, forms its
model-based Wald statistic, then calibrates the gene-statistic distribution
with a robust empirical null. It does not combine guide p-values by
Fisher's or Stouffer's method.
}
\details{
For gene \eqn{g}, let \eqn{w_{gj} = \mathrm{SE}(\widehat\beta_{gj})^{-2}}.
The shared effect and raw statistic are
\deqn{\widehat\beta_g =
\frac{\sum_j w_{gj}\widehat\beta_{gj}}{\sum_j w_{gj}},\qquad
T_g = \widehat\beta_g\sqrt{\sum_j w_{gj}}.}{
beta_g = (sum_j w_gj beta_gj) / (sum_j w_gj),
T_g = beta_g * sqrt(sum_j w_gj).}
Its null center is the median score among control genes when enough are
supplied, and otherwise the median across all genes. The null scale is the
largest of \code{min_scale}, the all-gene MAD, and the control-gene tail scale.
The all-gene MAD assumes that fewer than half of genes are active.
}
\examples{
guides <- barcs_example_guides()
bb_gene_consistency(
  guides,
  control = startsWith(guides$gene, "control"),
  min_control_genes = 2
)
}
\seealso{
Other gene-level summaries: 
\code{\link{bb_gene_eb_moderate}()},
\code{\link{bb_gene_normal}()},
\code{\link{bb_gene_original}()},
\code{\link{bb_gene_partial_pool}()}
}
\concept{gene-level summaries}
