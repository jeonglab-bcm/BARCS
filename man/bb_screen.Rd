% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen.R
\name{bb_screen}
\alias{bb_screen}
\title{Apply beta-binomial regression guide by guide}
\usage{
bb_screen(
  counts,
  data,
  formula,
  term,
  totals = NULL,
  guide = rownames(counts),
  gene = NULL,
  min_total_count = 10,
  ncores = 1L,
  ...
)
}
\arguments{
\item{counts}{Guide-by-sample count matrix or data frame.}

\item{data}{Sample-level covariate data frame, one row per column of
\code{counts}, in the same order.}

\item{formula}{One-sided regression formula.}

\item{term}{Name of the model-matrix coefficient to report. It must match a
column of \code{model.matrix(formula, data)}.}

\item{totals}{Optional integer-valued library-size vector holding the
unfiltered mapped-guide total per sample. Defaults to the column sums of
\code{counts}, which is only correct when \code{counts} has not been filtered.}

\item{guide}{Optional guide identifiers. Defaults to the row names of
\code{counts}.}

\item{gene}{Optional gene identifier per guide. When supplied, a \code{gene}
column is prepended and the result can be passed to the \verb{bb_gene_*()}
summaries.}

\item{min_total_count}{Guides whose summed count falls below this receive
missing results.}

\item{ncores}{Number of forked workers on Unix-like systems. Windows always
uses one worker.}

\item{...}{Additional arguments passed to \code{\link[=bbreg]{bbreg()}}.}
}
\value{
A data frame with one row per guide and columns \code{guide}, the
reported coefficient's \code{estimate}, \code{std_error}, \code{t_value}, \code{df} and
\code{p_value}, the fitted dispersion \code{rho}, the untruncated binomial Pearson
statistic \code{pearson_null}, the guide's \code{mean_cpm}, a \code{converged} flag, and
the Benjamini-Hochberg \code{fdr}.
}
\description{
Runs \code{\link[=bbreg]{bbreg()}} on every row of a count matrix against a shared sample
design, reports one prespecified model-matrix coefficient per guide, and
adds a Benjamini-Hochberg false discovery rate across guides.
}
\details{
Guides whose total count falls below \code{min_total_count}, and guides whose
fit errors, yield an all-missing row with \code{converged = FALSE} rather than
aborting the screen.
}
\examples{
set.seed(4)
design <- data.frame(
  day = rep(c(0, 7, 14), each = 3),
  replicate = factor(rep(1:3, 3))
)
totals <- rep(60000L, nrow(design))
simulate_guide <- function(slope) {
  rbinom(nrow(design), totals, plogis(-7 + slope * design$day / 14))
}
counts <- rbind(
  geneA_sg1 = simulate_guide(-1.2),
  geneA_sg2 = simulate_guide(-1.0),
  geneB_sg1 = simulate_guide(0),
  geneB_sg2 = simulate_guide(0)
)

bb_screen(
  counts = counts,
  data = design,
  formula = ~ I(day / 14) + replicate,
  term = "I(day/14)",
  totals = totals,
  gene = sub("_sg[0-9]+$", "", rownames(counts))
)
}
\seealso{
\code{\link[=bb_calibrate_controls]{bb_calibrate_controls()}} and \code{\link[=bb_moderate_dispersion]{bb_moderate_dispersion()}} to
recalibrate these tests, and the \verb{bb_gene_*()} functions to summarise
them by gene.

Other guide-level modelling: 
\code{\link{bb_contrast}()},
\code{\link{bbreg}()},
\code{\link{bbreg-methods}}
}
\concept{guide-level modelling}
