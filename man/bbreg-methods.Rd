% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bbreg.R
\name{bbreg-methods}
\alias{bbreg-methods}
\alias{coef.bbreg}
\alias{vcov.bbreg}
\alias{fitted.bbreg}
\alias{residuals.bbreg}
\alias{summary.bbreg}
\alias{print.summary.bbreg}
\title{Methods for fitted beta-binomial regressions}
\usage{
\method{coef}{bbreg}(object, ...)

\method{vcov}{bbreg}(object, ...)

\method{fitted}{bbreg}(object, ...)

\method{residuals}{bbreg}(object, type = c("response", "pearson"), ...)

\method{summary}{bbreg}(object, ...)

\method{print}{summary.bbreg}(x, digits = max(3L, getOption("digits") - 3L), ...)
}
\arguments{
\item{object, x}{A fitted \code{bbreg} object (\code{x} for \code{print.summary.bbreg()},
which takes the object returned by \code{summary()}).}

\item{...}{Ignored, present for S3 consistency.}

\item{type}{Residual scale: \code{"response"} returns the observed minus fitted
proportion, \code{"pearson"} divides it by the fitted beta-binomial standard
deviation.}

\item{digits}{Number of significant digits used when printing.}
}
\value{
\code{coef()} a named coefficient vector; \code{vcov()} the coefficient
covariance matrix; \code{fitted()} the fitted proportions; \code{residuals()} a
residual vector; \code{summary()} an object of class \code{summary.bbreg}; and
\code{print()} its argument, invisibly.
}
\description{
Standard extractors for objects returned by \code{\link[=bbreg]{bbreg()}}.
}
\examples{
set.seed(2)
design <- data.frame(dose = rep(c(0, 1), each = 5))
total <- rep(20000L, 10)
count <- rbinom(10, total, plogis(-6 + 0.8 * design$dose))
fit <- bbreg(count, total, ~ dose, design)

coef(fit)
vcov(fit)
head(residuals(fit, type = "pearson"))
summary(fit)
}
\seealso{
Other guide-level modelling: 
\code{\link{bb_contrast}()},
\code{\link{bb_screen}()},
\code{\link{bbreg}()}
}
\concept{guide-level modelling}
