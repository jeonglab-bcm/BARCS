% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bbreg.R
\name{bbreg}
\alias{bbreg}
\title{Fit beta-binomial regression for one guide}
\usage{
bbreg(
  count,
  total,
  formula,
  data,
  maxit = 100L,
  tolerance = 1e-08,
  mu_bound = 1e-08
)
}
\arguments{
\item{count}{Guide counts, one value per sample.}

\item{total}{Total library counts, one value per sample. These are the
unfiltered mapped-guide totals and must not be recomputed after guide
filtering.}

\item{formula}{One-sided mean-model formula, such as \code{~ dose + batch}.}

\item{data}{Sample-level covariates, one row per element of \code{count}, in the
same order.}

\item{maxit}{Maximum feasible-IRLS iterations.}

\item{tolerance}{Relative convergence tolerance.}

\item{mu_bound}{Numerical bound applied to fitted proportions.}
}
\value{
An object of class \code{bbreg}: a list whose most useful elements are
\code{coefficients}, \code{coefficient_table} (estimate, standard error, t value,
degrees of freedom, p-value), \code{covariance}, \code{rho}, \code{df.residual}, and
\code{converged}. Methods are provided for \code{\link[=coef]{coef()}}, \code{\link[=vcov]{vcov()}}, \code{\link[=fitted]{fitted()}},
\code{\link[=residuals]{residuals()}}, and \code{\link[=summary]{summary()}}.
}
\description{
Fits the logit mean model \verb{logit(mu) = x'beta} to one guide's counts by
feasible iteratively reweighted least squares, under the beta-binomial
variance \verb{n mu (1 - mu) \{1 + (n - 1) rho\}}. The intraclass correlation
\code{rho} is estimated from the Pearson estimating equation at each iteration,
and coefficients are tested against a Student t reference on residual
degrees of freedom.
}
\details{
The degrees of freedom come from the number of independent sequenced
libraries minus the rank of the design, never from read depth. Sequencing
a library more deeply shrinks the binomial part of the variance but buys no
additional degrees of freedom.
}
\examples{
set.seed(1)
design <- data.frame(
  dose = rep(c(0, 0.5, 1), each = 4),
  batch = factor(rep(c("a", "b"), 6))
)
total <- rep(50000L, nrow(design))
mu <- plogis(-6.5 + 1.1 * design$dose)
count <- rbinom(nrow(design), total, mu)

fit <- bbreg(count, total, ~ dose + batch, design)
summary(fit)
coef(fit)
}
\seealso{
\code{\link[=bb_contrast]{bb_contrast()}} to test a linear combination of coefficients, and
\code{\link[=bb_screen]{bb_screen()}} to apply this fit across a whole count matrix.

Other guide-level modelling: 
\code{\link{bb_contrast}()},
\code{\link{bb_screen}()},
\code{\link{bbreg-methods}}
}
\concept{guide-level modelling}
